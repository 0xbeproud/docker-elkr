input {
	redis {
	    host => "localhost"
        port => 6379
        codec => json
        data_type => "list"
        key => "filebeat"
    }
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "localhost:9200"
		user => logstash_internal
        password => changeme
	}
}
